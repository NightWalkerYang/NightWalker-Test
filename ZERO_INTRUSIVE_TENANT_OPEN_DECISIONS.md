# 零侵入租户待确认事项

这份文档只保留仍需补材料、拍板或继续收敛的问题。

后续新增事项时，建议按下面格式追加：

- 问题
- 当前默认建议
- 阻塞程度
- 需要谁拍板或补料

## 1. 通联聚合接入材料

当前状态：

- 已确认后续可以提供 `appid`、商户号、交易密码、RSA 私钥、RSA 通联公钥

后续还需要：

- 对应 API 文档
- 回调约定
- 测试环境说明

阻塞程度：

- 对支付闭环联调是直接阻塞
- 对 skill、文档重组、非支付模块不是阻塞
