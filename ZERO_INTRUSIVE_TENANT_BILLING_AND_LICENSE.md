# 零侵入租户计费与授权模式

这份文档只回答：

- 公有云模式的钱包、充值、扣费
- 本地部署模式的授权、续期、到期只读
- 两种模式并存时的边界

## 公有云模式

当前确认口径：

- 最终统一按人民币 `CNY` 结算
- `1 积分 = 1 人民币`

第一阶段扣费设计原则：

- 先做可执行、可解释、可审计
- 不假装已经拿到了 OpenClaw 核心内部的绝对精确成本

## 公有云模式计费真值

当前真实计费真值优先级如下：

1. 命中本地静态单价的 provider/model
   - 直接按 usage token 本地计费
   - 先按供应商币种计算，再按本地汇率折算为 `CNY`

2. usage 明细里带有 `cost.total`
   - 继续优先复用源系统 usage 明细

3. usage 明细缺少 per-record `cost.total`
   - 回退读取同一会话 `sessions.json` 里的 `estimatedCostUsd`
   - 先按本地 `USD -> CNY` 汇率折算
   - 再按同会话内 `total_tokens` 占比分摊

4. session 级 `estimatedCostUsd` 也不可用
   - 允许 sidecar 再按 usage token 与本地静态单价或 runtime 模型价格估算

当前明确：

- sidecar 只做同步、记账、积分扣减
- 不依赖在线价格查询
- 暂不把 coding plan 当第一阶段主计费口径

## 公有云充值与支付闭环

当前在线支付实际接法为：

- 通联 `4.1 H5收银台`

闭环为：

1. 创建充值订单
2. 钱包页展示可扫码二维码
3. 手机扫码后打开同源跳转页并自动提交到通联收银台
4. 通联异步回调自动确认
5. 钱包页可手动查询订单状态补确认
6. 支付成功后钱包入账
7. 租户管理员把钱包积分划转到 Agent

部署要求：

- `OPENCLAW_TENANT_PLATFORM_PUBLIC_BASE_URL`
- `OPENCLAW_TENANT_PAYMENT_ALLINPAY_*`

必须显式透传到 `openclaw-tenant-platform` 容器。

公网支付场景下：

- 这里必须使用真实可访问 `https://...`
- 不能只停留在宿主机 `.env`

## 通联接入实现要点

当前实现约定：

- H5 下单默认生产地址：
  - `https://syb.allinpay.com/apiweb/h5unionpay/unionorder`
- 交易查询默认地址：
  - `https://vsp.allinpay.com/apiweb/tranx/query`
- 签名字段名：
  - `signtype`

通联同步回跳页不能带查询参数，因此当前零侵入实现改为：

- `/tenant-platform-api/v1/public/payment/allinpay/return`

再由该页跳回：

- `/?ocTenantView=tenant-wallet`

如果 `notify_url` 走 `https`：

- 默认要用 `443`
- 若当前部署还没有 `443` 入口，则必须显式指定一个可被通联访问的地址

## 租户管理员页面语义

公有云模式下：

- 租户管理员完全不允许自行配置模型凭证
- 钱包、充值、耗量、流水都属于租户控制面能力

当前页面规则：

- `模型耗用` 只展示 `usage_charge`
- `钱包流水` 展示充值入账、Agent 划转扣减和 Agent 撤回回退
- `划转积分` 当前从钱包页移到 `Agent -> 已有Agent`

## 本地部署模式

第一阶段本地部署只做：

- 授权
- 到期控制
- 用量统计

明确不做：

- 在线充值
- 积分扣费

客户公司自行管理：

- 大模型厂商 API Key
- coding plan

我们不介入客户本地模型凭证管理。

## 本地部署授权控制

第一阶段本地部署只控制：

- 系统可用性
- 到期时间

当前规则：

- 到期后允许只读查看历史和统计
- 禁止继续发送消息
- 平台和租户页面都要显式区分本地版与公有云版

授权页当前要求：

- 显示到期日期
- 显示剩余天数
- 提前 7 天开始预警
- 最后 7 天按天提醒一次
- 支持续期码
- 支持重新部署并导入新的授权文件

续期方式允许共存：

- 续期码
- 重新部署并导入新的授权文件

日常续期优先：

- 续期码

## 公有云与本地版共存边界

当前要求不是做两套完全不同系统，而是在同一零侵入底座上区分：

- 公有云模式：钱包与扣费主语义
- 本地版：授权与到期主语义

当前实现应始终保证：

- 页面提示词不混淆
- 权限与入口不混淆
- 计费与授权状态不混淆

## 当前阅读建议

遇到这些问题时优先读本文件：

- 钱包为什么这么扣
- `cost.total` 缺失时怎么回退
- Allinpay 闭环应该怎么走
- 本地版为什么不扣积分
- 到期后为什么只能只读
